---
import Content from '../../components/Content.astro';
import CommonLayout from '../../layouts/CommonLayout.astro';
import { linkCss } from '../../styles/common';

// use this only to generate a list of pages, as it fucks up the starfield js, loads it to early on nav
// interface CustomDataFile {
//   url: string;
// }

// const pages = await Astro.glob<CustomDataFile>('./*.astro');
// const urls = pages.map((page) => page.url);
---

<CommonLayout title="Blog">
  <Content>
    <!-- <ul>
      {
        urls.map((url) => (
          <li>
            <a class={linkCss} href={url}>
              {url.split('/').pop()}
            </a>
          </li>
        ))
      }
    </ul> -->
    <ul>
      <li>
        <a class={linkCss} href="/test/img">img</a>
      </li>
      <li>
        <a class={linkCss} href="/test/misc">misc</a>
      </li>
      <li>
        <a class={linkCss} href="/test/pattern">pattern</a>
      </li>
      <li>
        <a class={linkCss} href="/test/ratio">ratio</a>
      </li>
      <li>
        <a class={linkCss} href="/test/starfield">starfield</a>
      </li>
    </ul>
  </Content>
</CommonLayout>
