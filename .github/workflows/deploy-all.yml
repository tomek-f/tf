name: deploy all

on:
    workflow_dispatch:

# jobs:
#   deploy-next-playground:
#     name: vercel
#     uses: tomek-f/tf/.github/workflows/deploy-next-playground.yml@master
#   deploy-react-playground:
#     name: vercel
#     uses: tomek-f/tf/.github/workflows/deploy-react-playground.yml@master
#   deploy-tomekf:
#     name: vercel
#     uses: tomek-f/tf/.github/workflows/deploy-tomekf.yml@master

jobs:
    deploy-next-playground:
        name: 🤘🏼
        uses: ./.github/workflows/deploy-next-playground.yml
        secrets: inherit
    deploy-react-playground:
        name: 🤘🏼
        uses: ./.github/workflows/deploy-react-playground.yml
        secrets: inherit
    deploy-tomekf:
        name: 🤘🏼
        uses: ./.github/workflows/deploy-tomekf.yml
        secrets: inherit
