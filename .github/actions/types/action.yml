name: 'Types action'
description: 'Runs a composite Types action'

runs:
  using: 'composite'
  steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      with:
        node-version: 18
        check-latest: true
        cache: 'yarn'
    - run: yarn install --immutable
      shell: bash
    - run: yarn turbo:check-types
      shell: bash
